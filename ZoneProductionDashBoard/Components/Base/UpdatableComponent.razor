@ChildContent

@code {
    public virtual void Update()
    {
        InvokeAsync(() =>
                        StateHasChanged()
            );
    }
        
    [Parameter]
    public RenderFragment? ChildContent { get; set; }
}